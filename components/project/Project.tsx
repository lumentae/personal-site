import Card from "../container/Card";

type ProjectProps = {
    name: string;
    description?: string;
    stars?: number;

};

export default function Project(props: ProjectProps) {
    return (
        <>
            
        </>
    );
}